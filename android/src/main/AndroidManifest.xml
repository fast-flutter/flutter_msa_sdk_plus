<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for OAID functionality -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    
    <!-- Required for some device manufacturers -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Required for Huawei devices -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    
    <!-- Required for Xiaomi devices -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    
    <!-- Required for OPPO devices -->
    <uses-permission android:name="android.permission.READ_LOGS" />
    
    <!-- Required for vivo devices -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    
    <!-- Required for Meizu devices -->
    <uses-permission android:name="android.permission.READ_SMS" />
    
    <!-- Required for Samsung devices -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
</manifest>
